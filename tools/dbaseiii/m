#!/bin/bash
export BUILD_LIBDIR=${CCCDIR}/usr/lib

# dbaseiii.so behuzza a fullscreen konyvtarakat
# ha az nem megfelelo, akkor dbaseiii.lib-et kell linkelni;
# a memokezeleshez linkelni kell a btbtx tablaobjektumot

c++app.sh -sccc${CCCVER}_dbaseiii

