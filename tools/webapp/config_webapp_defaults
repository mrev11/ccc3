
#interface to listen (default: localhost)
iface=127.0.0.1

#port to listen (default: 45678)
port=45678

#use SSL (default: true)
ssl=true

#private key (default: weabappdemo.pem)
keyfile=webappdemo.pem

#webapp to start (also use "weabapp_start" config file)
start=whello.exe

#content-encoding
gzip=true

#sec-websocket-extensions
permessage-deflate=true
