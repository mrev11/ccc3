

function main()

local n
local maplen:=8
local map0:=bitmap(maplen)
local map:=bitmap(maplen)


    for n:=1 to maplen
        bitmap.set(map,n,.t.)
    next
    

    ? bitmap.true(map)

    ?
    
    
    