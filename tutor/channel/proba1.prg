

function main()

local channel

    SET CHANNEL(channel) to  log-proba1 additive    // megnyitja
    SET CHANNEL(channel) on                         // aktiv
    
    ? "öt szép szűzlány őrült írót nyúz"            // ir bele (console-ba, printer-be, stb. nem ir!)
    
    SET CHANNEL(channel) off                        // inaktiv
    SET CHANNEL(channel) to                         // lezarja
    
    SET CHANNEL off



